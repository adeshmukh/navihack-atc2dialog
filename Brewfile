tap "hashicorp/tap"

# Install Terraform CLI from the official HashiCorp tap.
brew "hashicorp/tap/terraform"

# Install Google Cloud SDK with its cask distribution.
cask "google-cloud-sdk"

# Install mkcert for creating locally-trusted certificates.
brew "mkcert"

